using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    void Start()
    {
        //Astar.MinHeap heap = new Astar.MinHeap();
        //heap.RemoveFirst();
        //heap.Add(2, new Coordinate(2, 2));
        //heap.Add(10, new Coordinate(10, 10));
        //heap.Add(1, new Coordinate(1, 1));
        //heap.Add(3, new Coordinate(3, 3));
        //int size = heap.Size;
        //for (int i = 0; i < size; i++)
        //{
        //    Coordinate temp = heap.First();
        //    Debug.Log(temp.x);
        //    heap.RemoveFirst();
        //    Debug.Log("size:" + heap.Size);
        //}
    }
}
